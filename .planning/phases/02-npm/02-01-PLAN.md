---
phase: 02-npm
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - README.md
  - pnpm-lock.yaml
autonomous: false
requirements:
  - DIST-01
  - DIST-03
user_setup:
  - service: npm
    why: "将 ccusage-cn 发布至 npm 公共注册表"
    env_vars:
      - name: NPM_TOKEN
        source: "npmjs.com -> Access Tokens -> Generate (Classic, Publish)"
    dashboard_config:
      - task: "启用 2FA 认证（可选但推荐）"
        location: "npmjs.com -> Account -> Two-Factor Authentication"
must_haves:
  truths:
    - "package.json 已配置为可发布状态（private 已移除、version 1.0.0、元数据完整、prepublishOnly 就绪）"
    - "README.md 提供中英双语使用指导，覆盖安装、使用、环境变量、上游差异、兼容版本"
    - "发布前自动运行测试套件（npm test）和 publint 包质量检查"
    - "npm 包 ccusage-cn 已成功发布至 npm 公共注册表，可通过 npm view 查看"
    - "macOS 平台 bunx ccusage-cn 可正常运行（已验证）"
    - "Linux/Windows 兼容性在 README 中声明"
  artifacts:
    - path: "package.json"
      provides: "npm 发布配置与包元数据"
      contains: '"version": "1.0.0"'
      not_contains: '"private": true'
    - path: "README.md"
      provides: "项目文档（中英双语）"
      min_lines: 60
    - path: "node_modules/.bin/publint"
      provides: "包质量验证工具"
  key_links:
    - from: "package.json"
      to: "npm registry"
      via: "npm publish"
    - from: "package.json"
      to: "npm test && publint"
      via: "prepublishOnly script"
    - from: "bin/cli.js"
      to: "upstream ccusage binary"
      via: "spawner.js + binary-resolver.js"
---
<objective>
将 ccusage-cn 发布至 npm 公共注册表，完成三平台兼容性声明与验证。本阶段交付：已发布的 npm 包 `ccusage-cn@1.0.0`、中英双语 README、发布前质量门（prepublishOnly）。

Purpose: 让 macOS/Linux/Windows 用户可通过 `bunx ccusage-cn` 开箱使用，费用以人民币显示。
Output: 更新后的 `package.json`、新 `README.md`、安装的 `publint`、已发布的 npm 包、macOS 端到端验证。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-npm/02-CONTEXT.md
@.planning/phases/02-npm/02-RESEARCH.md
@package.json
@README.md（如果已存在，则读取；否则跳过）
</context>

<tasks>

<task type="auto">
  <name>Task 1: 更新 package.json 发布配置与元数据，并安装 publint</name>
  <files>package.json, pnpm-lock.yaml</files>
  <read_first>
    - package.json（当前待修改的文件）
    - .planning/phases/02-npm/02-CONTEXT.md（决策 D-02, D-04, D-09, D-10, D-11）
  </read_first>
  <action>
    修改 package.json 的具体操作如下（按顺序执行，不遗漏任何一项）：

    a) 删除顶层 `"private": true` 字段（整行连同逗号），per D-02。
    b) 修改 `"version": "0.1.0"` 为 `"version": "1.0.0"`，per D-09。
    c) 在 `scripts` 对象中添加：
       ```json
       "prepublishOnly": "npm test && publint"
       ```
       保留已有的 `"test": "vitest run"`。两条 script 是 scripts 对象的唯二成员，中间用英文逗号分隔。per D-03/D-04。
    d) 在 `devDependencies` 对象中添加：
       ```json
       "publint": "^0.3.12"
       ```
       保留已有的 `"vitest": "^3.0.0"`。per D-03 和 RESEARCH.md「publint ^0.3.12」。
    e) 在 `engines` 同级（顶层对象，scripts 之后、publishConfig 之前），添加以下字段按此顺序：
       - `"description": "ccusage 的人民币（CNY）适配版本 — 分析 AI 编程工具 Token 用量，费用以人民币展示"`
       - `"keywords": ["ccusage", "token", "usage", "ai", "cli", "cn", "cny", "chinese", "renminbi"]`
       - `"repository": {"type": "git", "url": "git+https://github.com/user/ccusage-cn.git"}`
       - `"license": "MIT"`
       - `"homepage": "https://github.com/user/ccusage-cn#readme"`
       per D-11。`repository.url` 使用 `git+` 前缀（npm 标准格式）。由于当前无 git remote 配置，使用占位 URL，用户可在创建仓库后替换。

    f) 不修改以下字段：`name`、`type`、`bin`、`files`、`dependencies`、`engines`、`publishConfig`。

    修改完成后，运行 `pnpm install` 以更新 pnpm-lock.yaml 并安装 publint。
  </action>
  <acceptance_criteria>
    - Source assertion: `grep -c '"private": true' package.json` 返回 0
    - Source assertion: `grep -c '"version": "1.0.0"' package.json` 返回 1
    - Source assertion: `grep -c '"prepublishOnly"' package.json` 返回 1
    - Source assertion: `grep -c '"publint"' package.json` 返回 1
    - Source assertion: `grep -c '"description"' package.json` 返回 1
    - Source assertion: `grep -c '"MIT"' package.json` 返回 1
    - Source assertion: `grep -c '"repository"' package.json` 返回 1
    - Test command: `ls node_modules/publint/package.json` 成功（publint 已安装）
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/Bai/Downloads/同步空间/works/Code/ccusage-cn && test 0 -eq $(grep -c '"private": true' package.json) && test 1 -eq $(grep -c '"version": "1.0.0"' package.json) && test 1 -eq $(grep -c '"prepublishOnly"' package.json) && test 1 -eq $(grep -c '"publint"' package.json) && ls node_modules/publint/package.json > /dev/null 2>&1</automated>
  </verify>
  <done>package.json 发布配置完成：private 已移除、version 1.0.0、prepublishOnly 已设置、publint 已安装、元数据完整。</done>
</task>

<task type="auto">
  <name>Task 2: 创建中英双语 README.md</name>
  <files>README.md</files>
  <read_first>
    - .planning/phases/02-npm/02-CONTEXT.md（决策 D-05, D-06, D-08）
  </read_first>
  <action>
    在项目根目录创建 README.md。per D-05/D-06/D-08，文档结构如下：

    - **语言策略**: 中文为主（先出现），英文为辅（紧随中文段落）。每个章节先写中文段落，再接英文段落。per D-05。
    - **章节结构（per D-06）**, 顺序不可调换：

      1. **项目简介 / Introduction**: 第一句中文引语概括核心价值（如「AI 编程 Token 费用，人民币一目了然」）。说明 ccusage-cn 是 ccusage 的人民币（CNY）适配版本。英文段落紧随其后。

      2. **安装方式 / Installation**: 列出 `bunx ccusage-cn`、`npx ccusage-cn`（无需安装，直接运行）、`npm install -g ccusage-cn`（全局安装）。每种给出一条具体命令。

      3. **使用示例 / Usage Examples**: 至少包含 4 个示例：
         - `ccusage-cn -b` — 查看当前周期账单
         - `ccusage-cn blocks --active` — 查看活跃会话区块费用
         - `ccusage-cn --json` — JSON 模式（costUSD 保留 + costCNY 追加）
         - `CCUSAGE_CNY_RATE=7.25 ccusage-cn -b` — 自定义汇率

      4. **环境变量 / Environment Variables**: 用表格列出 `CCUSAGE_CNY_RATE`（可选，数字，覆盖默认汇率），注明默认值 7.2 和三层回退策略（env var > CDN cache > 7.2 default）。

      5. **与上游差异 / Differences from Upstream**: 核心差异：所有 `$X.XX` → `¥Y.YY`；JSON 模式追加 `costCNY`；其余 100% 兼容。per D-08，在此注明：「macOS (ARM64) 已完成完整功能验证；Linux 和 Windows 声明兼容（基于上游 optionalDependencies 覆盖和跨平台代码分析），完整的三平台 CI 验证矩阵将在后续阶段实施」。

      6. **兼容的上游版本 / Compatible Upstream Version**: 依赖 `ccusage@^20.0.0`（caret 范围），自动兼容 patch/minor。ccusage-cn 使用独立版本号 `1.0.0`，不与上游版本号关联。

    - 末尾添加许可声明：`MIT License — 与上游 ccusage 一致`。
    - 禁止添加：CI 徽章、贡献指南、变更日志、测试说明（Phase 3 的内容）。
  </action>
  <acceptance_criteria>
    - Source assertion: `wc -l README.md` >= 60
    - Source assertion: README.md 包含全部 6 个章节的中文标题（"项目简介"、"安装方式"、"使用示例"、"环境变量"、"与上游差异"、"兼容的上游版本"）
    - Source assertion: README.md 包含 CCUSAGE_CNY_RATE 环境变量说明
    - Source assertion: README.md 包含 macOS/Linux/Windows 兼容性声明
    - Source assertion: `grep -c 'MIT License' README.md` >= 1
    - Negative assertion: README.md 不包含 CI 徽章语法（如 `![]`）
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/Bai/Downloads/同步空间/works/Code/ccusage-cn && test -f README.md && test $(wc -l < README.md) -ge 60 && grep -q '项目简介' README.md && grep -q '安装方式' README.md && grep -q '使用示例' README.md && grep -q '环境变量' README.md && grep -q '与上游差异' README.md && grep -q '兼容的上游版本' README.md && grep -q 'CCUSAGE_CNY_RATE' README.md && grep -q 'MIT License' README.md</automated>
  </verify>
  <done>README.md 中英双语文档创建完成，覆盖所有 6 个必要章节和三平台兼容性声明。</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: 运行发布前验证，然后执行 npm 登录与发布</name>
  <files>无（不修改文件）</files>
  <read_first>
    - .planning/phases/02-npm/02-CONTEXT.md（决策 D-01, D-07）
    - .planning/phases/02-npm/02-RESEARCH.md（Code Examples: npm publish 流程）
  </read_first>
  <action>
    【自动化部分 — 由 Claude 自动执行】

    在执行用户交互步骤之前，按以下顺序自动运行所有验证：

    1. 运行 `npm run prepublishOnly`，验证 `npm test`（vitest run，26 个用例）和 `publint` 均通过。任何失败立即中止，不进入下一步。
    2. 运行 `npm pack --dry-run` 预览 tarball 内容，确认包含以下文件：`bin/cli.js`、`src/*.js`、`package.json`、`README.md`。确认不包含 `test/`、`.planning/`、`pnpm-lock.yaml`。
    3. 输出验证结果摘要供用户参考。

    【用户交互部分 — 由 human-action checkpoint 处理】

    完成自动化验证后，展示结果给用户，然后提示用户执行以下操作：

    a) 检查 npm 登录状态：运行 `npm whoami`。如果未登录，提示用户执行 `npm login`（交互式）。
    b) 执行 `npm publish --access public` 发布包。
    c) 发布后执行 `npm view ccusage-cn` 确认包在 registry 中可见。

    <!-- 自动化验证结果 -->
  </action>
  <acceptance_criteria>
    - Behavior assertion: `npm run prepublishOnly` 退出码为 0
    - CLI output: `npm pack --dry-run` 输出中包含 `bin/cli.js`、`src/`、`package.json`、`README.md`
    - CLI output: `npm pack --dry-run` 输出中不包含 `test/`、`.planning/` 目录
    - Behavior assertion: `npm whoami` 返回有效的 npm 用户名
    - Behavior assertion: `npm publish --access public` 退出码为 0
    - Test command: `npm view ccusage-cn version` 返回 `1.0.0`
    - Test command: `npm view ccusage-cn description` 输出与 package.json 一致
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/Bai/Downloads/同步空间/works/Code/ccusage-cn && npm run prepublishOnly && npm pack --dry-run 2>&1 | grep -q 'bin/cli.js' && npm pack --dry-run 2>&1 | grep -v 'test/' | grep -v '.planning/' && npm whoami && npm publish --access public && npm view ccusage-cn version | grep -q '1.0.0'</automated>
  </verify>
  <done>ccusage-cn@1.0.0 已成功发布至 npm 公共注册表，npm view 确认可见。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 开发者工作站 → npm registry | npm publish 从本地到远程 registry 的认证数据传输 |
| npm registry → 用户 bunx/npx | 用户安装时从 npm registry 拉取包和二进制依赖 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | npm publish 发布内容 | mitigate | `files` 白名单（bin/, src/, package.json, README.md）限制发布范围；`npm pack --dry-run` 在 prepublishOnly 前验证 tarball 内容 |
| T-02-02 | Information Disclosure | npm 账户凭据 | mitigate | `npm whoami` 检查登录状态；推荐用户启用 npm 2FA |
| T-02-03 | Spoofing | npm 包名抢注 | accept | `ccusage-cn` 包名通过 `npm view` 验证为 404（可用），发布后 npm 保护该名称 |
| T-02-SC | Tampering | 依赖安装 | mitigate | publint [OK] 和 vitest [SUS]（已确认为合法包误报），审计表已验证。无新包引入 |
</threat_model>

<verification>
1. `npm run prepublishOnly` 退出码 0（npm test & publint 均通过）
2. `npm pack --dry-run` 输出包含必需文件，不包含 test/.planning 目录
3. `npm view ccusage-cn version` 返回 `1.0.0`
4. `npm view ccusage-cn description` 非空
5. README.md 行数 >= 60，包含全部 6 个章节
</verification>

<success_criteria>
1. package.json 已配置为可发布状态（private 移除、version 1.0.0、prepublishOnly 脚本、完整元数据）
2. README.md 中英双语文档创建完成，覆盖 D-06 所有 6 个章节和三平台兼容性声明（per D-08）
3. publint 已安装并可运行
4. `npm test` 和 `publint` 验证通过
5. `npm pack --dry-run` tarball 内容验证通过
6. npm 包 `ccusage-cn@1.0.0` 已成功发布至 npm 公共注册表
7. macOS 平台已验证（npm test + 发布流程），Linux/Windows 在 README 中声明兼容
</success_criteria>

<output>
创建 `.planning/phases/02-npm/02-01-SUMMARY.md` 记录执行结果
</output>
